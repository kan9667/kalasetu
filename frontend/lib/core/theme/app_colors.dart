import 'package:flutter/material.dart';

/// Warm, earthy, craft-inspired color palette evoking Indian textile and pottery traditions
class AppColors {
  // Primary colors - terracotta/deep clay orange
  static const terracotta = Color(0xFFD4785B);
  static const terracottaLight = Color(0xFFE89A7E);
  static const terracottaDark = Color(0xFFB85A3A);

  // Secondary - deep indigo
  static const indigo = Color(0xFF2F4858);
  static const indigoLight = Color(0xFF4A6A7C);
  static const indigoDark = Color(0xFF1A2C3A);

  // Accent - turmeric yellow
  static const turmeric = Color(0xFFFDB833);
  static const turmericLight = Color(0xFFFFCC66);
  static const turmericDark = Color(0xFFE5A51F);

  // Forest green accent
  static const forestGreen = Color(0xFF2D5016);
  static const forestGreenLight = Color(0xFF4A7A2C);
  static const forestGreenDark = Color(0xFF1A3009);

  // Backgrounds - handmade paper textures
  static const background = Color(0xFFFAF8F3);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF5F1E8);

  // Text colors - high contrast for accessibility
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF5A5A5A);
  static const textTertiary = Color(0xFF8A8A8A);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // State colors
  static const success = Color(0xFF4A7A2C);
  static const warning = Color(0xFFFDB833);
  static const error = Color(0xFFD44C4C);
  static const info = Color(0xFF4A6A7C);

  // Status badges
  static const statusLive = Color(0xFF4A7A2C);
  static const statusPending = Color(0xFFFDB833);
  static const statusDraft = Color(0xFF8A8A8A);

  // Dividers and borders
  static const divider = Color(0xFFE0DDD5);
  static const border = Color(0xFFD0CCC0);

  // Overlay and shadows
  static const overlay = Color(0x40000000);
  static const shadow = Color(0x20000000);

  // Offline indicator
  static const offline = Color(0xFF8A8A8A);

  AppColors._();
}
